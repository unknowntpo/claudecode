services:
  smallpond:
    build: .
    ports:
      - "8265:8265"  # Ray dashboard
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    environment:
      - RAY_DASHBOARD_HOST=0.0.0.0
